# Model Files

The trained news classifier (`models/news_classifier/`) is **not** in the repo (too large for GitHub).

To generate the model locally:

```bash
python scripts/train_news_classifier.py --epochs 3 --output models/news_classifier
```

See `scripts/README_TRAINING.md` for full instructions.
